/**
 * Animated loading spinner.
 * @returns Spinner element.
 */
export function LoadingSpinner() {
  return <div className="h-5 w-5 animate-spin rounded-full border-2 border-slate-500 border-t-transparent" />;
}
